# #!/usr/local/env bash
set -euo pipefail
source $(dirname "$0")/src/data
source $(dirname "$0")/src/func
validateConfig || err $LINENO "validateConfig" "main"

directoryCheck || err $LINENO "directoryCheck" "main"
moveLog || err $LINENO "moveLog" "main"


