# vProx minimal runtime source manifest
# Purpose: canonical include set for lean build/runtime packaging.
#
# Include these files/directories when producing a minimal runtime source bundle.
# Paths are repo-relative.
#
# NOTE: Geolocation support is NOT included in the default minimal set.
# See the optional section at the bottom of this file if you need it.

.env.example
LICENSE
README.md
Makefile
go.mod
go.sum
vprox.service.template

cmd/
internal/
chains/chain.sample.toml

# Optional – NOT part of the default minimal set.
# Uncomment to include geolocation support out-of-the-box:
# ip2l/ip2location.mmdb
