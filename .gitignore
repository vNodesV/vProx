# macOS
.DS_Store
.AppleDouble
.LSOverride

# Icon must end with two \r
Icon

# Thumbnails
._*

# Files that might appear in the root of a volume
.DocumentRevisions-V100
.fseventsd
.Spotlight-V100
.TemporaryItems
.Trashes
.VolumeIcon.icns
.com.apple.timemachine.donotpresent

# Directories potentially created on remote AFP share
.AppleDB
.AppleDesktop
Network Trash Folder
Temporary Items
.apdisk

# Go
# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib
vProx

# Local build output (optional `make build`)
.build/

# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool
*.out
coverage.*

# Dependency directories (if using vendor)
vendor/

# Go workspace file
go.work
go.work.sum

# Logs
logs/
*.log

# Temporary files
tmp/

# Local admin helper scripts (do not commit)
scripts/harden_repo_settings.sh
scripts/set_required_checks.sh

# Local collaboration and agent artifacts (private/local-only)
# Note: `.github/agents/` is repo configuration for Copilot agents and should be tracked.
agents/*.*
agents/projects/*.*
docs/
!docs/UPGRADE.md
.github/agents/*.*
.github/CODEOWNERS
.github/dependabot.yml
AGENT_DIRECTIVE.md
doc.go
scripts/*.*

# Environment files
.env
!.env.example

# IDE and editor files
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store



# GeoLocation database — keep compressed .gz, ignore uncompressed
ip2l/ip2location.mmdb
